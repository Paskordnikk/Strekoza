# Инструкция по деплою на Render.com

## 🚀 Настройка сервера на Render.com

### 1. Подготовка проекта

Убедитесь, что в вашем репозитории есть:
- `server.py` - основной файл сервера
- `requirements.txt` - зависимости Python
- Папка `strm/` с данными высот (если нужна)

### 2. Создание сервиса на Render.com

1. Зайдите на [render.com](https://render.com)
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите ваш репозиторий GitHub
4. Выберите репозиторий с вашим проектом

### 3. Настройка сервиса

#### Основные настройки:
- **Name**: название вашего сервиса (например, `karta-api`)
- **Environment**: `Python 3`
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `python server.py`

#### Важно для Start Command:
Если используете аргументы командной строки, команда должна быть:
```bash
python server.py --password "ваш_пароль" --secret-key "ваш_секретный_ключ"
```

**Но лучше использовать переменные окружения** (см. ниже)!

### 4. Настройка переменных окружения ⭐ (РЕКОМЕНДУЕТСЯ)

В настройках сервиса на Render.com перейдите в раздел **"Environment"** и добавьте:

#### Обязательные переменные:
- **`SITE_PASSWORD`** = `ваш_безопасный_пароль` (пароль для доступа к сайту)
- **`JWT_SECRET_KEY`** = `ваш_длинный_случайный_ключ` (секретный ключ для JWT)

#### Автоматические переменные (Render.com устанавливает сам):
- **`PORT`** - автоматически устанавливается Render.com (не нужно настраивать вручную)

#### Как сгенерировать безопасный JWT_SECRET_KEY:
```python
import secrets
print(secrets.token_urlsafe(32))
```

Или в Python:
```python
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

### 5. Настройка плана

- **Free Plan**: бесплатный план, сервис "засыпает" после 15 минут неактивности
- **Starter Plan**: $7/месяц - сервис всегда активен

### 6. Перезапуск сервера

#### Способ 1: Через Dashboard (самый простой)
1. Зайдите на [dashboard.render.com](https://dashboard.render.com)
2. Найдите ваш сервис
3. Нажмите кнопку **"Manual Deploy"** → **"Deploy latest commit"**
   - Или нажмите **"Restart"** для быстрого перезапуска

#### Способ 2: Через Git push
1. Внесите изменения в код
2. Закоммитьте и запушьте в GitHub:
   ```bash
   git add .
   git commit -m "Update server"
   git push origin main
   ```
3. Render.com автоматически перезапустит сервис

#### Способ 3: Через API (если нужно)
```bash
curl -X POST https://api.render.com/v1/services/{service_id}/deploys \
  -H "Authorization: Bearer {your_api_key}" \
  -H "Content-Type: application/json"
```

### 7. Просмотр логов

1. Зайдите в ваш сервис на Render.com
2. Перейдите в раздел **"Logs"**
3. Вы увидите логи запуска и работы сервера
4. При старте должно быть видно: `[INFO] Пароль установлен: переменная окружения (SITE_PASSWORD)`

### 8. Обновление переменных окружения

Если нужно изменить пароль или секретный ключ:

1. Зайдите в настройки сервиса → **"Environment"**
2. Измените значение переменной
3. Нажмите **"Save Changes"**
4. Нажмите **"Restart"** для применения изменений

⚠️ **Важно**: После изменения пароля или `JWT_SECRET_KEY` все существующие токены станут невалидными. Пользователям нужно будет войти заново.

### 9. Настройка фронтенда

Убедитесь, что в `script.js` и `login.js` правильно указан URL API:

```javascript
const API_URL = window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1' 
    ? 'http://127.0.0.1:8000' 
    : 'https://your-service-name.onrender.com';
```

Замените `your-service-name.onrender.com` на ваш реальный URL с Render.com.

### 10. Проверка работы

После деплоя проверьте:

1. **Health Check**: 
   - Откройте `https://your-service.onrender.com/docs` (FastAPI автоматически создает документацию)
   - Должна открыться страница Swagger UI

2. **API Endpoint**:
   - Откройте `https://your-service.onrender.com/api/login`
   - Должен вернуться JSON с ошибкой (так как нет пароля в запросе) - это нормально

3. **Фронтенд**:
   - Откройте ваш сайт на GitHub Pages
   - Попробуйте войти с паролем
   - Должен работать вход

### 11. Troubleshooting

#### Проблема: Сервер не запускается
- Проверьте логи в Render.com
- Убедитесь, что `requirements.txt` содержит все зависимости
- Проверьте, что команда запуска правильная

#### Проблема: 401 Unauthorized
- Проверьте, что переменная `SITE_PASSWORD` установлена
- Проверьте логи сервера - должно быть видно источник пароля

#### Проблема: CORS ошибки
- Убедитесь, что в `server.py` CORS настроен правильно (разрешен `*` для всех источников)

#### Проблема: Сервер "засыпает" на Free плане
- Это нормально для Free плана
- Первый запрос после "сна" может занять 30-60 секунд
- Рассмотрите переход на Starter Plan ($7/месяц) для постоянной работы

### 12. Безопасность

⚠️ **Важно для продакшена**:

1. ✅ Используйте переменные окружения для паролей и секретных ключей
2. ✅ Никогда не коммитьте пароли в Git
3. ✅ Используйте сильные пароли (минимум 16 символов)
4. ✅ Регулярно обновляйте зависимости
5. ✅ Мониторьте логи на подозрительную активность

---

## 📝 Краткая инструкция по перезапуску

**Быстрый перезапуск:**
1. Зайдите на dashboard.render.com
2. Найдите ваш сервис
3. Нажмите **"Restart"**

**Перезапуск с обновлением кода:**
1. Внесите изменения
2. `git add . && git commit -m "Update" && git push`
3. Render.com автоматически перезапустит

**Перезапуск с изменением переменных:**
1. Измените переменные в настройках
2. Нажмите **"Save Changes"**
3. Нажмите **"Restart"**

