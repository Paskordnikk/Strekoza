# Пошаговая инструкция: Добавление переменных окружения на Render.com

## 🎯 Шаг 1: Откройте раздел Environment

1. Зайдите на [dashboard.render.com](https://dashboard.render.com)
2. Выберите ваш сервис (Web Service)
3. В левом меню найдите раздел **"Environment"** и кликните на него

## 🔑 Шаг 2: Добавьте переменную SITE_PASSWORD

1. В секции **"Environment Variables"** нажмите кнопку **"Add"**
2. Появится форма с двумя полями:
   - **Key** (Ключ): введите `SITE_PASSWORD`
   - **Value** (Значение): введите ваш пароль (например, `my-secure-password-123`)
3. Нажмите **"Save Changes"** (или кнопку сохранения)

## 🔐 Шаг 3: Добавьте переменную JWT_SECRET_KEY

1. Снова нажмите кнопку **"Add"** в секции "Environment Variables"
2. Заполните форму:
   - **Key** (Ключ): введите `JWT_SECRET_KEY`
   - **Value** (Значение): введите длинный случайный ключ

### Как сгенерировать безопасный JWT_SECRET_KEY:

**Вариант 1: В PowerShell (Windows)**
```powershell
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

**Вариант 2: В командной строке**
```cmd
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

**Вариант 3: В Python интерактивно**
```python
import secrets
print(secrets.token_urlsafe(32))
```

3. Скопируйте сгенерированный ключ (длинная строка из случайных символов)
4. Вставьте его в поле **Value**
5. Нажмите **"Save Changes"**

## ✅ Шаг 4: Проверьте результат

После добавления обеих переменных вы должны увидеть список:

```
Environment Variables
├── SITE_PASSWORD        [●●●●●●●●●●●●]  (значение скрыто)
└── JWT_SECRET_KEY       [●●●●●●●●●●●●]  (значение скрыто)
```

Render.com автоматически скрывает значения переменных для безопасности (показывает только точки).

## 🔄 Шаг 5: Перезапустите сервис

1. Перейдите в раздел **"Events"** или вернитесь на главную страницу сервиса
2. Нажмите кнопку **"Restart"** в правом верхнем углу
3. Дождитесь перезапуска (обычно 1-2 минуты)

## 📋 Шаг 6: Проверьте логи

После перезапуска:

1. Перейдите в раздел **"Logs"**
2. Найдите строку при старте сервера:
   ```
   [INFO] Пароль установлен: переменная окружения (SITE_PASSWORD), длина пароля: X символов
   ```
3. Если видите эту строку - всё настроено правильно! ✅

## 🎨 Визуальная схема интерфейса

```
┌─────────────────────────────────────────────────┐
│  Environment                                    │
├─────────────────────────────────────────────────┤
│                                                 │
│  Environment Variables                          │
│  ┌───────────────────────────────────────────┐  │
│  │ Key              Value                   │  │
│  ├───────────────────────────────────────────┤  │
│  │ SITE_PASSWORD    [●●●●●●●●●●●●]  [Edit] │  │
│  │ JWT_SECRET_KEY   [●●●●●●●●●●●●]  [Edit] │  │
│  └───────────────────────────────────────────┘  │
│                                                 │
│  [+ Add]  ← Нажмите здесь для добавления       │
│                                                 │
└─────────────────────────────────────────────────┘
```

## ⚠️ Важные замечания

1. **Не используйте простые пароли** - минимум 12-16 символов, буквы, цифры, спецсимволы
2. **JWT_SECRET_KEY должен быть длинным** - минимум 32 символа, используйте `secrets.token_urlsafe(32)`
3. **Значения скрываются автоматически** - Render.com не показывает реальные значения переменных
4. **После изменения переменных нужно перезапустить** - иначе изменения не применятся

## 🔧 Редактирование существующих переменных

Если нужно изменить значение:

1. Найдите переменную в списке
2. Нажмите на кнопку **"Edit"** (или иконку карандаша) справа от переменной
3. Измените значение в поле **Value**
4. Нажмите **"Save Changes"**
5. **Обязательно перезапустите сервис** для применения изменений

## 🗑️ Удаление переменных

Если нужно удалить переменную:

1. Найдите переменную в списке
2. Нажмите на кнопку **"Delete"** (или иконку корзины) справа от переменной
3. Подтвердите удаление
4. **Перезапустите сервис**

## 🆘 Troubleshooting

**Проблема: Не вижу кнопку "Add"**
- Убедитесь, что вы находитесь в разделе "Environment" вашего сервиса
- Проверьте, что у вас есть права на редактирование сервиса

**Проблема: Переменные не применяются**
- Убедитесь, что нажали "Save Changes" после добавления
- Перезапустите сервис (кнопка "Restart")
- Проверьте логи - должно быть видно источник пароля

**Проблема: Забыл пароль или ключ**
- Значения переменных скрыты в интерфейсе Render.com
- Нужно создать новые значения и обновить переменные
- После изменения `JWT_SECRET_KEY` все существующие токены станут невалидными

---

## 📝 Краткая шпаргалка

```
1. Environment → Add → SITE_PASSWORD = "ваш_пароль"
2. Environment → Add → JWT_SECRET_KEY = "сгенерированный_ключ"
3. Save Changes
4. Restart сервиса
5. Проверить логи
```

